@using Kendo.Mvc.UI
@model decimal?

@(Html.Kendo().NumericTextBoxFor(m => m)
      .HtmlAttributes(new { style = "width:100%" })
      .Format(ViewData["format"]?.ToString() ?? string.Empty)
      .Min(0)
      .Max(null))