Must create "Uploads" virtual directory to save uploaded files - share with WebService(write in same directory).
Must config "OpenDataDirectory" virtual directory to save open data exports.
Must to config ImagePreprocessiong -> write permission to cache directory. By default cache dir is create in "App_Data";
Create file log directory with permission to application write
For prod change tawk.to script with the prod one which u can find in _layout.cshtml